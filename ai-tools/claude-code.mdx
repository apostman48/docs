---
title: JSON Viewer — Full Demo
description: Interactive, collapsible JSON viewer using a Mintlify snippet.
---

import JsonViewer, {
  demoObject,
  demoJson,
  demoLargeArray,
  demoInvalidJson
} from "/snippets/json-viewer.jsx";

# JSON Viewer Demo

---

## 1) Object via `data`

<JsonViewer data={demoObject} collapsedAt={1} />

---

## 2) JSON string via `json` (fully expanded)

<JsonViewer json={demoJson} collapsedAt={0} />

---

## 3) Large array (deterministic values)

<JsonViewer data={demoLargeArray} collapsedAt={1} />

---

## 4) Invalid JSON handling

<JsonViewer json={demoInvalidJson} />
