---
title: JSON Viewer — Full Demo
description: Interactive, collapsible JSON viewer using a Mintlify snippet.
---

import JsonViewer, { DEMO_JSON } from "/snippets/json-viewer.jsx";

# JSON Viewer Demo

<JsonViewer json={DEMO_JSON} collapsedAt={1} />

---

## Fully expanded example

<JsonViewer json={`{
  "status": "ok",
  "version": "2.1.0",
  "endpoints": [
    {"path": "/v2/users", "method": "GET"},
    {"path": "/v2/projects/:id", "method": "PATCH"}
  ],
  "limits": {"rps": 15, "burst": 60}
}`} collapsedAt={0} />
