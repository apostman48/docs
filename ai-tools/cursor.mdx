---
title: JSON Viewer — Full Demo
description: Interactive, collapsible JSON viewer using a Mintlify snippet.
---

import JsonViewer from "/snippets/json-viewer.jsx";

// Define stable data outside the JSX props
const sampleData = {
  meta: { requestId: "req_9c1e2c0f", receivedAt: "2025-09-07T10:31:00Z" },
  user: {
    id: 123456,
    name: { first: "Ada", last: "Lovelace" },
    roles: ["admin", "analyst"],
    profile: { bio: "Mathematician", lastLoginAt: null }
  },
  projects: [
    { id: "prj_1", name: "Analytical Engine", stats: { issuesOpen: 12 } },
    { id: "prj_2", name: "Notes on Babbage", stats: { issuesOpen: 0 } }
  ]
};

const sampleJson = `{
  "status": "ok",
  "version": "2.1.0",
  "endpoints": [
    {"path": "/v2/users", "method": "GET"},
    {"path": "/v2/projects/:id", "method": "PATCH"}
  ]
}`;

const largeArrayData = {
  results: Array.from({ length: 10 }).map((_, i) => ({
    id: i + 1,
    ok: (i % 3) !== 0,
    score: ((i * 137) % 1000) / 10
  })),
  paging: { page: 1, pageSize: 10, total: 5000 }
};

const invalidJson = `{ "oops": true, trailing: }`;

# JSON Viewer Demo

This page demonstrates the `JsonViewer` snippet with multiple data sources.

---

## 1) Object via `data`

<JsonViewer data={sampleData} collapsedAt={1} />

---

## 2) JSON string via `json` (fully expanded)

<JsonViewer json={sampleJson} collapsedAt={0} />

---

## 3) Large array (deterministic values)

<JsonViewer data={largeArrayData} collapsedAt={1} />

---

## 4) Invalid JSON handling

<JsonViewer json={invalidJson} />
