import { JsonViewer } from "/snippets/json-viewer.jsx"

<JsonViewer 
  initialData={{
    "api": {
      "version": "2.0",
      "endpoints": [
        {
          "path": "/users",
          "method": "GET",
          "description": "Get all users"
        },
        {
          "path": "/users/{id}",
          "method": "GET", 
          "description": "Get user by ID"
        }
      ]
    }
  }}
  allowInput={false}
/>